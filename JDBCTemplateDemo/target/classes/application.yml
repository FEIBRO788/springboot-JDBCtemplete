spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver  # 使用新版驱动类
    url: jdbc:mysql://localhost:3306/tedu?serverTimezone=GMT%2B8&characterEncoding=utf8
    username: root  # 修正拼写和缩进
    password: root   # 确保冒号后加空格